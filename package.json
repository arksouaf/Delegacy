{
  "name": "delegacy",
  "version": "0.1.0",
  "private": true,
  "description": "Session Key Wallet Infrastructure — as a Service. ERC-4337 + Solana powered.",
  "author": "delegacy",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.1.0"
}
