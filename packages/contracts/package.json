{
  "name": "@sessionguard/contracts",
  "version": "0.1.0",
  "description": "SessionGuard ERC-4337 smart contracts — account, validators, and policies",
  "license": "MIT",
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "clean": "forge clean",
    "deploy:local": "forge script script/Deploy.s.sol --rpc-url http://localhost:8545 --broadcast",
    "deploy:sepolia": "forge script script/Deploy.s.sol --rpc-url $SESSIONGUARD_RPC_URL --broadcast --verify"
  }
}
