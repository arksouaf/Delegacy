[package]
name = "session-guard"
version = "0.1.0"
description = "SessionGuard — Session key infrastructure for Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "session_guard"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
